# Core dependencies first
numpy
packaging
tqdm
Pillow

# PyTorch ecosystem (must be installed before xformers)
torch
torchvision

# JAX ecosystem
jax
flax
optax

# Hugging Face ecosystem
transformers
huggingface_hub
diffusers
datasets
accelerate

# Optional/extension packages
bitsandbytes
wandb

# (Run separately) xformers last (requires torch to be already installed)